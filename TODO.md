# Fuel Path TODO

## Next Decision

- [ ] P0 stabilisation pass before new feature expansion: restore visible demo/native behaviour, reduce branch/tree mess, and keep adding only review gates or simplifications until Plan, Nearby, Account and web-demo smoke flows are clean. Current trigger: web-demo map station logos regressed into oversized/clipped marker images because local asset cache keys were stale and Leaflet image rules overrode marker image sizing. Evidence now added in `tests/api/web-demo-safety.test.js`: local CSS/JS cache keys are versioned and map price-pin brand images must be visibly bounded.
- [ ] Clear current beta readiness blockers before treating Fuel Path as safe for real-user beta testing: provider terms for NSW/ACT, QLD and TAS; iOS validation; privacy/store evidence; and support readiness. Android physical-device evidence is now clear on Pixel 9 Pro. Summary gate: `npm run check:beta-readiness -- --api-base https://fuel-path.vercel.app --store-evidence-json docs/templates/STORE-PUBLISHING-EVIDENCE.template.json --provider-terms-evidence-json docs/templates/PROVIDER-TERMS-EVIDENCE.template.json --allow-blocked`. The summary now reports ordered next actions across provider, native, store and support blockers instead of hiding later work behind the first blocker.
- [ ] P0 monetisation validation instrumentation: turn route savings into measured behaviour before any paid claim. User story: as Leo, I need evidence that users repeatedly plan routes, save commutes, opt into route alerts and open navigation from recommendations, so monetisation decisions are based on behaviour rather than interest. Success metrics: repeated route plans, saved commute creation, alert opt-ins and navigation-open events are captured with privacy-safe aggregate reporting; the report separates commuter/high-frequency users from casual tests; no precise Home/Work coordinates, raw route coordinates, push tokens or provider secrets are logged. Break-it evidence: `npm run check:monetisation-behaviour -- --evidence-json docs/templates/MONETISATION-BEHAVIOUR-EVIDENCE.template.json --allow-blocked` blocks empty evidence, and `tests/api/monetisation-behaviour-evidence.test.js` proves sensitive fields, missing event evidence and threshold failure are rejected. Design artefacts: `docs/monetisation-behaviour-instrumentation-plan-2026-07-01.md` and `docs/templates/MONETISATION-BEHAVIOUR-EVIDENCE.template.json`.
- [ ] P1 B2B/fleet validation artefact: build one shareable route fuel savings report for 5-10 recurring routes before building fleet SaaS. User story: as a small fleet or high-frequency operator, I need a simple weekly-style report showing route fuel savings, detour cost, approved-brand/fleet-card assumptions and provider limitations, so I can judge whether a pilot is worth discussing. Success metrics: report covers 5-10 realistic recurring routes, labels all assumptions, shows pump price versus adjusted price, avoids guaranteed savings claims, and produces at least one concrete pilot/willingness-to-pay follow-up before this moves beyond validation. Break-it evidence: create a sample report or generator fixture plus a review note showing the report does not overclaim savings, policy compliance or data coverage. Validation artefacts: `docs/fleet-lite-route-savings-report-validation-2026-07-01.md` and `docs/templates/FLEET-LITE-SAVINGS-REPORT.template.md`.
- [ ] P2 EV provider-readiness boundary: keep EV route planning as prototype/cautious until provider pricing, coverage, availability semantics and commercial terms are approved. User story: as an EV or mixed-fleet user, I need charger discovery to clearly state directory/prototype limits, so I do not treat incomplete provider data as live bay availability or reliable route-stop optimisation. Success metrics: EV provider readiness documents cover pricing, rate limits, Australian and NT coverage, connector/power fields, live availability semantics, attribution, caching and commercial display rights; the UI and docs avoid live-availability, tariff or route-optimisation claims unless a provider contract supports them. Break-it evidence: keep a provider-readiness checklist and static/user-facing copy guard that blocks production-grade EV claims while provider evidence is missing. Boundary artefact: `docs/ev-provider-readiness-boundary-2026-07-01.md`.
- [x] Prepare API.NSW unblock plan and credential validation script.
- [x] Register/subscribe for API.NSW Fuel API credentials.
- [x] Validate registered API.NSW credentials and live v1 price endpoint.
- [x] Validate QLD Fuel Prices Direct Outbound API production access.
- [x] Add local server-side proxy for live API.NSW scoring in the web demo.
- [x] Prepare validation demo pack and paste-ready API.NSW support note.
- [x] Stop and capture strategic reflection against initial objectives.
- [x] Run synthetic validation dry run across target participant segments.
- [x] Research consumer fuel discounts, loyalty offers and fleet cards.
- [x] Add classic around-me map view with fuel and brand filters to the demo.
- [x] Convert route view into an interactive map with highlighted route stops and nearby context stations.
- [x] Split the web demo into top-level Plan Trip, Near me and Account experiences.
- [x] Add Account tab with demo registration-based vehicle profile setup.
- [x] Add configurable Google Maps provider with OpenStreetMap fallback.
- [x] Replace route/scenario controls with From and To address fields.
- [x] Add cost-aware explicit route planning and route-cache status.
- [x] Add saved-route alert profiles for commute and safe regional examples.
- [x] Record the three main project goals and Goal 1 build roadmap.
- [x] Merge whole-of-Australia roadmap, success metrics and break-it testing regime into project docs.
- [ ] Prove Goal 1: Best Fuel Decision, Not Another Map. Technical evidence gate is documented in `GOAL-1-EVIDENCE-GATE.md`; recruitment pack is documented in `VALIDATION-RECRUITMENT-PACK.md`; real recruited validation sessions remain required before this can be marked proven.
- [x] Build national provider capability matrix for NSW, ACT, QLD, VIC, SA, WA, TAS and NT.
- [x] Expose regional capability state in backend `/api/status` and user-facing empty/limited states.
- [x] Add provider contract tests for capability labels: live, limited, pending_access, fallback and unsupported.
- [x] Fix empty-result handling for long routes such as Sydney to Canberra.
- [x] Connect ranked results and map markers both ways.
- [x] Simplify the recommendation card into one clear action, reason and trust cue.
- [x] Add locked-provider `wait` action gating so wait advice can appear only from official tomorrow-price evidence, not demo/prediction data.
- [x] Strengthen source, freshness, availability and eligibility warnings in the recommendation flow.
- [x] Add adaptive route-corridor scoring fallback and regression coverage.
- [x] Confirm ACT feed coverage is exposed through the live API.NSW FuelCheck feed.
- [x] Run first internal validation pass against the current prototype.
- [x] Fix invalid typed-route fallback so unresolved addresses do not show sample-corridor recommendations.
- [ ] Run real validation sessions with recruited participants.
- [ ] Confirm permitted app/commercial usage for FuelCheck data. Production NSW/ACT and TAS live routing is fail-closed until `FUEL_PATH_NSW_ACT_USAGE_TERMS_CONFIRMED=1`, `FUEL_PATH_TAS_USAGE_TERMS_CONFIRMED=1` or shared `FUEL_PATH_FUELCHECK_USAGE_TERMS_CONFIRMED=1` is set after written terms confirmation. Evidence gate: `npm run check:provider-terms -- --api-base https://fuel-path.vercel.app` reports NSW, ACT, QLD and TAS terms blockers separately from VIC/NT access blockers.
- [x] Build a small sample-data web demo around the existing route-scoring engine.
- [x] Add discount rules fixture to the web demo.
- [x] Show pump price, confirmed user price and possible lower price in demo search results.
- [x] Make mobile wallet copy explicitly separate confirmed price from possible lower price, with "possible only" and "not guaranteed" wording protected by mobile verification.
- [ ] Confirm whether the live web demo can be shared publicly after usage rights are confirmed. Production provider usage gates currently prevent unconfirmed NSW/ACT, QLD and TAS live prices from being served publicly.
- [x] Add internal validation address typeahead suggestions for suburbs, addresses and POIs.
- [x] Limit long-route ranked results to a decision-sized set with progressive expansion.
- [x] Make stale-price severity reduce recommendation confidence more strongly for fallback, sample and unknown-source data.
- [x] Treat official live provider timestamps as price effective/unchanged-since dates, not stale API-response age.
- [x] Add live brand/network discount rules on top of FuelCheck station data.
- [x] Add visible saved-route alert card in Plan.
- [x] Apply map-first Plan UI and capture design system direction.
- [x] Create first Expo / React Native shell for Nearby, Plan and Account using the local Fuel Path backend.
- [x] Smoke test native web preview against live NSW/ACT FuelCheck responses.
- [x] Smoke test native web preview across national capability cases, including live, limited, pending and unsupported regions.
- [x] Add working interactive map tiles and branded station pins to the Expo web preview.
- [x] Draw planned trips from real route geometry instead of a straight endpoint line.
- [x] Make Nearby location editable, defaulting to the Sylvania validation address and supporting independent suburb/address searches.
- [x] Rename Nearby sort controls to Closest, Cheapest and Best value, with Cheapest scoped to the current map view where available.
- [x] Convert Nearby to a full-map layout with a collapsible ranked-results sheet.
- [x] Add route-led map camera behaviour with dynamic breathing room and selected-station focus.
- [x] Add native `react-native-maps` provider behind the existing StationMap component for iOS/Android builds.
- [x] Configure production Google Maps Android API key and run device builds for Android map validation. Android preview APK `f67da375-ad46-484d-8fc8-d5fc71a52e07` installs and opens on the ARM64 AVD, and Google tiles render on Plan, Nearby and pan screenshots with no Maps warning lines. Evidence: `tmp/native-smoke/android-preview-smoke-2026-06-20T03-18-45-402Z.md`; Plan reports `blankMapLikely: false`, colour ratio `0.6849`, detail buckets `134`. Source-level iOS simulator validation is now captured separately in `tmp/native-smoke/ios-validation-2026-06-30T23-16-23-867Z.md`; signed iOS preview/development validation remains separate push/store evidence.
- [x] Replace browser-only current-location shim with Expo Location foreground permission flow for Nearby and Plan Trip.
- [x] Choose Google Places Autocomplete (New) as the preferred production address/POI provider behind the Fuel Path backend.
- [x] Make Plan Trip address lookup session-token aware for future production autocomplete billing.
- [x] Add backend `/api/geocode` provider adapter for Google, Mapbox, HERE, Geoapify and Nominatim fallback.
- [x] Add AU address-index-first lookup path so exact G-NAF-style addresses resolve before external providers.
- [x] Redesign Plan Trip route editor around modern mapping-app patterns: explicit From/To rows, clear controls, swap route, current-location row, rich suggestion rows, recents, saved routes and Home/Work shortcuts.
- [x] Expand Home/Work address management with direct typed edit and refine-on-map support.
- [x] Persist recent locations across app restarts with privacy-safe limits and remove controls.
- [x] Add route-editor accessibility pass for focus order, touch target sizing, dynamic text and screen-reader labels.
- [x] Add route-editor break-it test matrix covering current location denied, clear/retype, swap, weak lookup, exact address, POI, saved route, Home/Work and recents.
- [x] Add restrained route timing cue and wire it into the Plan recommendation card: show Fill today states, support future Wait if you can, hide neutral/no-cycle.
- [x] Implement Google Places Autocomplete (New) backend adapter contract behind billing/API-key gates.
- [x] Roll out cost-controlled national location lookup strategy. Product rule: G-NAF must be the primary Australian street-address index; Fuel Path local hints and station matches must be second; Google Places can only be a backend-controlled POI/hard-miss fallback with explicit quota, cache, session-token and billing gates.
- [x] Add `/api/status` location-lookup strategy metadata: primary provider, address-index mode, external fallback provider, paid fallback enabled/disabled, daily cap, cache policy and attribution.
- [x] Complete hosted G-NAF production path on Oracle Always Free or the cheapest acceptable paid alternative, using a dedicated hosted target and no generic app database. Evidence: Oracle VM `fuel-path-gnaf-vm` is running `VM.Standard.A1.Flex`, 1 OCPU, 6 GB memory and 180 GB boot volume; local Postgres contains `16,905,824` G-NAF address rows with all required indexes; Caddy serves `https://gnaf.152.69.175.222.sslip.io`; Vercel production has `FUEL_PATH_GNAF_API_URL` and sensitive `FUEL_PATH_GNAF_API_TOKEN`; production `FUEL_PATH_GNAF_DATABASE_URL` was removed to avoid fallback to the old 80,000-row staging DB. Hosted readiness evidence `tmp/gnaf-hosted-readiness-2026-06-19T20-59-public-api.json` passes row count, token rejection, index readiness and 8/8 exact regional smoke cases.
- [x] Add hosted G-NAF storage/cost review gate. Evidence: `npm run review:gnaf-hosted-storage -- --load-plan tmp/gnaf-hosted-load-plan-2026-06-19T16-25-01-136Z.json --require-passed` wrote `tmp/gnaf-hosted-storage-review-2026-06-19T16-25-39-760Z.md`, confirming the Oracle Always Free load attempt under a 180 GB boot volume, 200 GB free block-storage limit, no public Postgres, no paid load balancer and a USD 1 budget alert assumption. It is storage/cost evidence only, not hosted deployment or public launch evidence.
- [x] Add hosted G-NAF national-load plan gate. Evidence: `npm run plan:gnaf-hosted-load` inspects the raw ZIP, local national SQLite, dedicated hosted DB rows, hosted index state, target kind and latest storage review without writing data. Latest plan `tmp/gnaf-hosted-load-plan-2026-06-19T20-38-10-549Z.md` is `ready_to_load`: raw ZIP present, local SQLite has 16,905,824 rows across ACT, NSW, NT, OT, QLD, SA, TAS, VIC and WA, configured hosted target kind is `neon`, hosted smoke DB has 80,000 rows and all expected indexes, reviewed estimated hosted storage is 17.5-35 GB, and the generated national-load command includes `--storage-review`, `--progress-json` and `--run-id`. National hosted load remains to be executed against the intended Oracle/dedicated production target.
- [x] Add hosted G-NAF national-load progress and resume guard. Evidence: unbounded hosted loads now require `--progress-json`; setup failure writes a `failed` progress manifest; interrupted runs can resume with `--resume-progress-json`, skip already completed states and preserve completed-state counts; resume with `--reset` is refused. `tests/api/gnaf-load-runner.test.js` covers dry-run progress, required progress, setup-failure progress, state skipping and reset refusal.
- [x] Load the national G-NAF index into the intended hosted database, create search indexes and run hosted smoke checks for exact, prefix, unit/slash, townhouse, regional, rural and remote addresses. Evidence: Oracle-hosted Postgres has `16,905,824` rows; public API readiness passed; hosted preview smoke passed `20/20` in `tmp/geocode-hosted-preview-smoke-2026-06-19T20-50-09-349Z.json`; production `/api/geocode?q=87A%20Corea%20Street%20Sylvania&limit=5` returns the expected `fuel_path_gnaf` exact address. The previous invalid preview fixtures for WA unit, ACT townhouse and NT lot were replaced with real G-NAF-backed cases after production proved the invented premises did not exist in the index.
- [x] Change geocode auto mode so a Google key alone does not make Google the primary provider; paid fallback must require an explicit `FUEL_PATH_PAID_GEOCODE_FALLBACK_ENABLED` gate.
- [x] Add backend Google Places fallback cost guardrails: session-token requirement, AU-only requests, minimum query length, debounce-compatible cache, daily cap, hard fail-closed behaviour and field-mask restrictions. Evidence: mocked provider tests cover key-alone disabled state, explicit enablement, missing token, cap exhaustion, exact G-NAF skip, malformed payload degradation and cached repeat lookup.
- [x] Replace in-memory Google Places fallback daily cap with durable backend quota storage before enabling Google fallback in production. Evidence: `api/_geocodeQuotaStorage.js` reserves quota through Postgres when configured, `/api/status` exposes quota storage mode, production hardening fails closed without durable quota storage, and mocked Google tests cover durable cap exhaustion before provider calls.
- [x] Add lookup UX states for address source and fallback behaviour: exact G-NAF address, local station/place match, street/area-only fallback, broader external/provider result, no match, fallback disabled and quota/cost cap reached. Evidence: Plan, Nearby and Account suggestion rows now use shared source-confidence chips; selected Plan From/To fields show the same confidence detail; mobile verification includes a guard for exact, address, area, street/area, external and limited lookup labels.
- [x] Add break-it tests for lookup ordering and costs: exact G-NAF skips Google, POI hard miss can call Google only when enabled, Google disabled returns local/no-match, quota exceeded blocks Google, cache prevents repeat paid calls, and malformed provider payloads degrade safely.
- [x] Harden national From/To autocomplete fallback under provider outage. Evidence: focused geocode regressions cover state-suffixed capital/airport hints, disambiguated POI/airport prefixes, visible primary-name ranking over alias-only matches, generic regional town prefixes over airport/POI rows, explicit POI intent preservation, `Artamon` typo correction, and national regional towns across NSW, ACT, VIC, QLD, WA, SA, TAS and NT without provider calls; `npm run test:geocode-route-fields:local` now resolves 52/52 endpoints and 100/100 unique route pairs under forced Nominatim rate limiting, with median 3 chars to a proper suggestion, p90 8 chars to a top-1 proper suggestion, max 3 chars to any proper suggestion and route-pair top-1 max 20 chars. Broad 3-character prefixes deliberately stay broad instead of over-ranking specific POIs.
- [x] Add exact-address readiness report separating current seed/local fallback from true G-NAF-backed coverage. Evidence: `npm run test:geocode-exact-address-readiness:local` shows current configured seed mode resolves 2/16 exact address cases and falls back to locality for 14/16, while a temporary all-jurisdiction G-NAF SQLite fixture resolves 16/16 exact labels with 0 provider calls. The script marks current launch readiness as `not_ready_for_public_exact_address_claim` until a real configured index covers every case.
- [x] Add exact-address From/To route-field stress evidence. Evidence: `npm run test:geocode-route-exact-addresses:local` runs 100 unique route pairs across 16 national exact-address cases, including units, slash addresses, suffixes, townhouses, rural and remote towns. Current configured seed mode still resolves only the two NSW seed addresses exactly; temporary G-NAF fixture mode resolves 16/16 exact labels and 100/100 unique route pairs without provider calls. Latest report: `tmp/geocode-route-exact-address-field-stress-2026-06-19T19-38-49-283Z.md`. Fixture character counts are mechanics-only, not a public production precision claim.
- [x] Harden local national G-NAF SQLite lookup so developer/test exact-address runs do not stall on broad prefixes. Evidence: large SQLite search now skips broad prefixes, drops noisy road/unit tokens for FTS, preserves large unit numbers such as `Unit 1005`, and `npm run test:geocode-local-national-address-smoke` passed 32/32 real sampled national addresses from the 16.9M-row local SQLite with p90 30 chars and external provider blocked. This is local developer evidence only; hosted public launch still needs hosted G-NAF readiness and benchmark evidence.
- [x] Fix POI lookup coverage/ranking, then rerun the full national lookup benchmark. Evidence: regional POI full-name ranking now beats fuzzy hosted G-NAF address rows for benchmark-style POI queries; regressions cover Jenolan Caves, Broome International Airport and Willow Court New Norfolk. Production deployment `dpl_2wMnkbUeMozEGRxaEDM94JwA1j22` passed public readiness with `16,905,824` hosted rows and no blockers. Hosted 900-case benchmark `tmp/geocode-hosted-national-benchmark-2026-06-19T23-04-43-862Z.json` passed `900/900` top matches: `600/600` G-NAF addresses and `300/300` regional POIs, p90 any-match chars `18` overall, address `18`, POI `3`.
- [x] Add deploy/release lookup readiness gate so `/api/status` and `npm run check:lookup-readiness` block public exact-address claims unless hosted G-NAF row count, exact-address smoke, hosted national benchmark, cache policy and paid-fallback controls are all ready. Hosted benchmark evidence must be current and non-future-dated. Paid Google fallback readiness now requires durable quota storage, a tiny daily cap, confirmed key restrictions and a confirmed budget alert before it can be treated as ready.
- [x] Add lookup readiness evidence publisher so hosted readiness JSON plus hosted 900-case benchmark JSON can generate the exact env contract consumed by `npm run check:lookup-readiness`. Evidence: `tests/api/lookup-evidence-publisher.test.js` covers passing env generation, readiness-gate handoff, missing exact smoke, failed benchmark thresholds, unprovable benchmark timestamp and future-dated benchmark timestamp.
- [x] Add a lookup release evidence summary so local precision, exact-address mechanics, hosted preview and hosted national benchmark evidence are reviewed as one release decision. Evidence: `npm run summarise:lookup-release-evidence -- --lookup-readiness tmp/lookup-readiness-check-2026-06-19T23-04-43-862Z.json` wrote `tmp/lookup-release-evidence-summary-2026-06-19T23-48-22-810Z.md` with status `passed`, local precision ready and public launch ready. `tests/api/lookup-release-evidence-summary.test.js` covers blocked-local, passing-launch, hosted `/api/status` readiness handoff, enforced launch failure, ignoring named fixtures, ignoring named local prefix benchmark artefacts and rejecting local module-mode national benchmark artefacts as hosted launch evidence.
- [x] Add hosted G-NAF national-load plan as an explicit lookup release gate. Evidence: `npm run summarise:lookup-release-evidence` now consumes timestamped `tmp/gnaf-hosted-load-plan-*.json` evidence and reports storage/cost review, local national rows, hosted rows, row gap, estimated hosted storage and the generated progress-manifest national-load command before public launch review. Latest summary `tmp/lookup-release-evidence-summary-2026-06-19T20-38-10-509Z.md` has the load plan passing and is blocked only by configured exact-address coverage, hosted preview smoke and hosted national benchmark evidence; `tests/api/lookup-release-evidence-summary.test.js` covers review-required, ready-to-load and ignoring named fixture load plans.
- [x] Add repeatable local lookup-release runner. Evidence: `npm run release:lookup-local` now runs route-field stress, exact-address route-field stress, 600-prefix fallback benchmark, hosted G-NAF load-plan refresh and release summary in order; rendered Plan-field smoke is auto-included when the local app is reachable on port 8081, can be forced with `--plan-smoke`, and can be skipped with `--no-plan-smoke`. Latest precision pack plus current rendered Plan smoke wrote `tmp/lookup-release-evidence-summary-2026-06-19T16-28-02-298Z.md`; refreshed release summary after street/area precision UX, resolver hardening, hosted-load progress guard, hosted-evidence resolver hardening and 100-pair rendered Plan stress wrote `tmp/lookup-release-evidence-summary-2026-06-19T19-35-28-046Z.md`. Local precision is ready and public launch remains blocked by configured exact-address coverage and hosted benchmark evidence. `tests/api/lookup-local-release-runner.test.js` locks dry-run command order, auto-inclusion and explicit opt-out.
- [x] Improve rural, remote and island lookup coverage in the no-cost local fallback. Evidence: `npm run test:geocode-prefix-600:local` is now a regression gate across NSW, ACT, VIC, QLD, WA, SA, TAS and NT; it resolves 600/600 top matches under forced provider rate limiting, including 120/120 `rural_remote_island` cases, with 0 wrong suggestions and median 3 chars to any correct suggestion. Hosted national G-NAF remains the launch-grade exact-address coverage item.
- [x] Add hosted geocode preview smoke before the full national benchmark. Evidence: `npm run test:geocode-hosted-preview -- --api-base <hosted-url>` runs 20 high-risk exact-address, unit/slash, townhouse, rural, remote, island and POI cases and writes JSON plus Markdown evidence. `tests/api/geocode-hosted-preview-smoke.test.js` covers passing report generation, failing-after-artefact behaviour and diagnosis of hosted address rows that come from seed/local providers instead of `fuel_path_gnaf`; `tests/api/lookup-release-evidence-summary.test.js` verifies those diagnostics flow into release evidence.
- [x] Add quiet Plan-field guidance for partial street addresses when autocomplete has no suggestion yet. Evidence: mobile guard verifies the old noisy lookup errors stay out of the UI and partial street input shows `Add suburb or postcode to narrow the address.` This improves the exact-address dead-air moment but does not reduce backend characters-to-match; hosted G-NAF/provider autocomplete is still needed for faster exact street-address suggestions.
- [x] Prevent unsafe early street suggestions for exact-style and ambiguous broad street input while hosted G-NAF is unavailable. Evidence: local street hints now wait for suburb/postcode, explicit uppercase state code or a clear locality context on numbered and street-only queries; regional street fallback waits for a specific locality prefix; the cache key separates typed `Wa` from explicit `WA`; latest `npm run test:geocode-prefix-600:local` preserves 600/600 final top matches and reduces `wrongStreetTopBeforeAnyMatch` from 3 to 0.
- [x] Surface slow exact-address local-fallback categories in release evidence. Evidence: `npm run summarise:lookup-release-evidence` now adds JSON/Markdown attention items for precise-address and duplex/townhouse categories when p90 characters-to-correct-suggestion remains high, so local fallback can pass while launch UX risk stays visible.
- [x] Normalise route-planning address lookup failures into plain user guidance. Evidence: mobile API client maps provider/rate-limit/backend lookup failures to `We couldn't check that address right now. Add suburb or postcode, or try again shortly.` and the mobile guard keeps raw provider busy wording out of the UI.
- [x] Disable Plan route for unconfirmed address-like text before submit. Evidence: browser smoke showed validation lookup can rank a POI or nearby metro street above the intended exact address, so Plan now keeps the CTA disabled for typed street-address-like From/To fields until the user selects a suggestion; mobile guard verifies start/destination precision copy and fallback submit-time rejection remains in place.
- [x] Make validation-provider address suggestions visibly unconfirmed and less POI-biased. Evidence: mobile API ranking now demotes POI-like validation rows below numbered street rows for street-address queries; validation address rows show `Needs confirmation` with `Not an exact address match. Confirm this row before planning.`; direct address-like geocoding rejects these rows unless the user explicitly selects one.
- [x] Prevent typed-but-unselected route planning from silently accepting weak approximate area matches for address-like input. Evidence: `npm run test` in `mobile-app/` verifies `geocodeAddress` rejects low-confidence/approx-area automatic matches and asks the user to choose a suggestion or add suburb/postcode. Users can still deliberately choose an approximate suggestion row.
- [x] Add rendered Plan-field browser smoke for precision UX. Evidence: `npm run smoke:plan-fields` in `mobile-app/` runs 11 mobile-viewport browser cases against mocked API responses, covering blank/disabled state, partial street guidance, state-only address rejection, locality-qualified suggestion confirmation, validation-provider ranking, `Needs confirmation` evidence, `Street/area only` local/regional fallback evidence, airport pair route submit and edit recovery. The smoke now preflights the local app URL and gives a direct Expo start command when the app is not reachable. Latest run wrote `tmp/plan-field-browser-smoke-2026-06-19T19-28-51-139Z.md` with 11/11 passing against `http://127.0.0.1:8081/`.
- [x] Add rendered Plan-field browser stress for route-pair selection UX. Evidence: `npm run stress:plan-fields` in `mobile-app/` runs 100 unique national From/To route pairs against the rendered mobile Plan screen, covering all 52 endpoints across NSW, ACT, VIC, QLD, WA, SA, TAS and NT, with deterministic mocked geocode/route/score responses. Latest run wrote `tmp/plan-field-browser-stress-2026-06-19T19-24-02-329Z.md` with 100/100 pairs passing, 100 unique route pairs, 52/52 endpoint coverage, 200 geocode calls and 4 route submits. The stress generator now fails if route-pair uniqueness regresses.
- [x] Neutralise shared direct-address confirmation copy so Nearby does not mention route planning. Evidence: `geocodeAddress` now says `Choose a suggestion to confirm this address, or add suburb or postcode.` while Plan keeps route-specific pre-submit hints; `mobile-app/scripts/check-map-camera-guards.mjs` locks the split.
- [x] Add staged lookup deployment planner so local test, hosted preview, production smoke and paid fallback enablement cannot be blurred together. Evidence: `npm run plan:lookup-deploy -- --release-summary tmp/lookup-release-evidence-summary-2026-06-19T23-48-22-810Z.json --readiness tmp/lookup-readiness-check-2026-06-19T23-04-43-862Z.json` wrote `tmp/lookup-deployment-stages-2026-06-19T23-48-38-923Z.md` with status `ready`, next stage `paid_fallback` and no blockers. `tests/api/lookup-deployment-stages.test.js` covers blocked preview/production, fail-fast required stages, production-ready evidence, paid-fallback quota, tiny-cap, restricted-key and budget-alert blockers, all-controls-ready state and ignoring named unit-test summaries.
- [x] Deploy location lookup strategy in stages: local test, hosted preview smoke, production smoke, then optionally enable Google fallback with durable quota storage, tiny daily cap, restricted key and budget alert. Evidence: production `/api/status` now reports lookup readiness `ready`, public exact-address claims allowed, hosted G-NAF API mode, `16,905,824` rows, exact smoke passed, hosted benchmark passed, `900` cases, address top rate `1`, POI top rate `1`, address p90 chars `18`, POI p90 chars `3`; staged planner is ready with paid fallback still disabled.
- [x] Add QLD provider adapter after fuel ID and region mapping are confirmed.
- [x] Add first WA FuelWatch provider adapter for Perth/metro live Nearby and route scoring.
- [x] Expand WA FuelWatch provider to full live statewide v2 with request-budgeted region planning, per-region/product/day caching and tomorrow-price checks.
- [x] Apply for VIC Servo Saver Public API access and receive approval credentials.
- [ ] Implement VIC Servo Saver adapter after approved schema is available.
- [x] Add SA Fuel Pricing Information Scheme Direct API adapter behind server-side token configuration.
- [x] Confirm TAS fuel data/API access path. Evidence: API.NSW Fuel API v2 states NSW and TAS support for reference, prices, nearby, location and new-prices endpoints; `docs/03-provider-data/PROVIDER-ACCESS-READINESS.md` records the validation ask.
- [x] Implement TAS FuelCheck adapter through API.NSW v2. Evidence: live payload/schema validation passed on 19 June 2026 with approved API.NSW credentials; Hobart nearby returned 40 TAS record hints; backend adapter smoke returned `api_tas` with 20 usable U91 stations; provider-routing tests cover TAS capability and mocked v2 nearby payloads.
- [ ] Confirm NT MyFuel data/API access path and implement adapter after approved schema is available.
- [x] Add saved commutes, notification permission flow and backend alert sync path for validation builds.
- [x] Design backend push scheduler for price-triggered saved-route alerts.
- [x] Add backend saved-route alert storage and evaluator foundation with push delivery disabled.
- [x] Add clear backend saved-route alert outcome labels and summaries for send alert, watch only, skip alert, quiet today and range-first, with regression coverage while push delivery stays gated.
- [x] Add scheduled saved-route evaluator, protected cron endpoint, Expo delivery path and push receipt contract behind production env gates.
- [x] Connect scheduled evaluator to live route scoring and gated native push-token registration path.
- [x] Add strict native preflight for EAS project id, alerts sync token, physical-device API URL and Android Maps key.
- [x] Create and link EAS project `@leokessel/fuel-path`.
- [x] Configure EAS preview API URL and separate preview alert validation token.
- [x] Separate mobile preview alert validation token from the protected backend alerts write token.
- [ ] Run native device validation with EAS project id and alerts sync token before enabling Expo push delivery. Strict preview preflight passes; Android emulator validation is locally available through `Fuel_Path_Arm64_API_35`, and historical physical Android installed-preview validation passed on Pixel 9 Pro `49231FDAP0017N`. The current localParity APK needs a fresh physical-device smoke/performance pass before beta readiness can clear. Source-level iOS simulator validation now passes, but signed iOS preview/development, push-token and backend-sync validation remain pending. Evidence: historical Android `tmp/native-smoke/android-preview-smoke-2026-06-20T20-59-06-687Z.md`, historical Android summary `tmp/native-smoke/android-performance-summary-2026-06-20T21-12-06-531Z.md`, and iOS source-level `tmp/native-smoke/ios-validation-2026-06-30T23-16-23-867Z.md`.
- [x] P0 production hardening: disable sample/demo fallback as a recommendation source in production. Evidence: `production hardening disables demo fallback responses`.
- [x] P0 production hardening: complete explicit provider health, degraded-state and stale-cache response contracts for every station, route, geocode and alert path. Evidence: station, route, geocode and alert responses expose explicit provider/degraded/cache or delivery-mode state.
- [x] P0 production hardening: add single-flight provider fetches, stale-while-revalidate caching, retry budgets and circuit breakers for live provider calls. Evidence: live provider calls use single-flight fetches, timeout budgets, bounded retry attempts, stale-while-revalidate cache refresh and circuit-breaker cooldowns; regressions cover transient retry success and stale cache background revalidation.
- [x] P0 production hardening: make durable alert storage mandatory before backend saved-route alert sync or push delivery can be enabled. Evidence: `backend alert sync rejects ephemeral memory storage`.
- [x] P0 production hardening: add duplicate suppression, idempotency and delivery-audit checks for scheduled saved-route alerts under retry and cron overlap conditions. Evidence: `scheduled evaluator is idempotent across cron overlap and retry`.
- [x] P0 production hardening: add route-scoring spatial prefiltering before nearest-route checks so national station data does not scale as all-stations by all-route-points. Evidence: `route scoring prefilters stations outside the route envelope`.
- [x] P0 production hardening: split `_backend.js` into provider adapters, geocoding, routing, scoring, alert evaluation, capability status and shared response-contract modules. Provider runtime, provider credentials, route geometry, routing, route scoring, alert evaluation, alert orchestration, alert record normalisation, NSW FuelCheck adapter, WA FuelWatch adapter, QLD/SA FPP Direct adapter, geocode provider status, full geocoding engine and capability status are extracted. Evidence: full backend tests 93/93 pass after extracting `api/_geocode.js` and `api/_alertOrchestration.js`; 300-case geocode matrix and 300-case regional-hard matrix both pass 300/300 under forced provider rate limiting. VIC remains schema-blocked before adapter implementation.
- [x] P0 production hardening: treat OSRM and any validation provider fallback as visible capability state, not an invisible production substitute. Evidence: route status exposes no-cost validation mode and OSRM route payloads include `providerMode: "validation"`.
- [x] P1 performance hardening: add backend load/regression tests for provider outage, provider timeout, cold cache, long route, metro route, high station count and concurrent alert evaluation. Evidence: provider outage/cooldown, provider timeout, cold-cache degradation, route prefiltering, high-station scoring, route-provider long geometry, metro-route single-flight and concurrent alert overlap now have regression coverage.
- [x] P1 package-size hardening: add CI bundle-size and asset-size budgets for native preview builds and web export. Evidence: web export/source asset budgets run in `npm run verify`; native APK/AAB/IPA artifact budgets run through `npm run budget:native` when CI downloads an EAS preview artifact or sets `FUEL_PATH_NATIVE_ARTIFACT`. The default native budget check now reports only the newest artifact per platform type, while explicit `FUEL_PATH_NATIVE_ARTIFACT` checks remain exact.
- [x] P1 package-size hardening: compress app icons and audit brand-logo PNGs before store builds. Evidence: brand-logo bundle audit is done; temporary `npx oxipng` lossless optimisation reduced PNG assets from 1,076,550 bytes to 972,695 bytes, source assets now pass at 950.7 KB / 1.43 MB and brand icons at 467.3 KB / 878.9 KB.
- [x] P1 package-size hardening: replace long-tail bundled brand logos with generated initials, lazy remote icons or a small core logo set. Evidence: bundled brand logos reduced from 46 to 20 and web export fell from about 1.17 MB to 913 KB.
- [ ] P1 mobile performance hardening: broaden marker rendering, map panning, route redraw and station selection checks beyond the first physical pass. Android physical installed-preview smoke now passes on Pixel 9 Pro `49231FDAP0017N`: 186 rendered frames, 1 janky frame, 0.5% janky, p95 8 ms, non-blank Plan/Nearby/Nearby-after-pan map screenshots and no Maps warning lines. The smoke harness now treats zero-frame `gfxinfo` output as insufficient evidence instead of false jank, then performs an explicit measured gesture pass before reading frame counters. `scripts/check-beta-readiness.mjs` now sees Android physical-device and performance evidence as ready. Next step is either iOS simulator/device validation or a second lower-end Android pass before public performance claims.
- [ ] P1 mobile architecture hardening: split the large native screens into smaller route-editor, recommendation, map/list, saved-route alert and account settings modules. Plan extractions are done: `SavedCommuteShortcuts` owns the saved-commute shortcut strip, `QuickPlaceShortcuts` owns the Home/Work/recent route shortcut panel, `DecisionEvidencePanel` owns the recommendation evidence/trade-off metrics, `RouteAddressSuggestions` owns route address suggestion rows plus typed-address precision hints, `PlanRouteEditorCard` owns address inputs, current-location control, suggestion rendering, fuel picker and plan button, `PlanRouteSheet` owns the minimised map sheet, station detail panel, recommendation summary, empty states, save-commute action and suggested-stop list, and `useRouteAddressSuggestions` owns address suggestion loading/error/results plus session-token reset. Plan route edit handlers now share one `reopenRouteEditor` reset path for route controls, station detail and sheet minimisation while stale-response protection stays in `PlanScreen`. Nearby extractions are done: `NearbyLocationSearch` owns the location input, recent/suggested-location rows, evidence chips and current-location control, `NearbyStationSheet` owns the bottom sheet, selected-station card, sort controls, list and map recovery affordance, and `useNearbyLocationSearch` owns Nearby query, recent locations, suggestion loading/error/results and session-token reset. Account top-level extractions are done: `VehicleFuelCard` owns vehicle/fuel settings, `DecisionRuleCard` owns the lightweight saving/detour rule controls, `SavedPlacesCard` owns the Home/Work settings wrapper and saved-commute quick actions, `SavedPlaceEditor` owns saved-place search, current-location capture, map-centre refinement and saved-place confidence chips, `DiscountWalletCard` owns discount selection versus redemption state, `PolicyModeCard` owns policy-mode approved-brand settings, `SavedRouteAlertsCard` owns notification permission copy, saved-route watch/remove actions, sync timestamps and per-route saving/detour/tank rules, and `WeeklyReportCard` owns the driver-facing fleet-lite summary and no-payroll/no-admin boundary. `npm run arch:screens` now ratchets `PlanScreen.tsx` to 729 lines / 15 state hooks / 1 effect / 2 refs, `NearbyScreen.tsx` to 468 lines / 14 state hooks / 3 effects / 1 ref and `AccountScreen.tsx` to 115 lines / 0 state hooks. The guard prevents these screen files from growing while the larger splits remain pending, and `npm test` now runs it before map-camera guards. Success requires extracting focused modules, preserving current `npm run verify`, adding focused regression coverage for saved-route switching and route-editor recovery, and avoiding bundle-size growth.
- [ ] P1 cross-platform map accessibility/performance hardening: apply the web-demo marker lesson to native and web surfaces by keeping map pins from duplicating list semantics, measuring marker rendering under pan/route redraw, and documenting the accessible list/map contract. Web demo Leaflet pins are decorative and covered by `tests/api/web-demo-safety.test.js`; mobile web Leaflet station pins are decorative and covered by `npm run test:map-camera`; native `react-native-maps` station pins now hide from the accessibility tree while `StationRow` owns the labelled button semantics, guarded by `npm run test:map-camera`. Native pan/route-redraw performance has one passing Android physical smoke; real assistive-tech behaviour and iOS remain device-validation items.
- [ ] P1 database platform review: stress-test Supabase as a possible managed Postgres target before moving away from the current Oracle-hosted G-NAF API. Decision rule: do not migrate the 16,905,824-row G-NAF lookup layer just because the dashboard is nicer; require a like-for-like benchmark against the current production API, a storage/cost review for the full indexed data set, a connection/runtime plan that does not expose Postgres directly to the mobile app, backup/restore evidence, and a rollback plan. Current code recognises Supabase as its own hosted target kind in the G-NAF load planner, and `npm run check:database-platform -- --evidence-json docs/templates/DATABASE-PLATFORM-REVIEW.template.json --allow-blocked` now blocks the empty Supabase review until row load, index, benchmark, latency, storage/cost, official-source, connection, backup, ownership and rollback evidence is filled. Current production path remains Oracle API first.
- [ ] P0 secret hygiene: rotate any local or provider database credentials that have been pasted into chat/tooling output, then replace local `.env.local` with freshly scoped values. `.env.local` is ignored by Git, but live-looking database URLs should still be treated as exposed once they have appeared in logs or transcripts. `npm run check:secret-hygiene` now scans tracked plus untracked non-ignored files for live-looking database URLs, API keys and private key blocks while allowing documented placeholders and local/test URLs.
- [x] P1 customer-feedback hardening: add break-it cases for stale prices, wrong fuel grade, missing/out-of-fuel stations, location jump, map panel traps, duplicate/incorrect user edits and lost alert/redemption state. Evidence: stale non-official prices, wrong fuel grade, unavailable/out-of-fuel price recommendation cases, saved-route alert audit preservation, location-jump map camera guards, map panel recovery controls and persisted discount redemption state now have regression coverage.
- [x] P1 reliability hardening: add source/freshness/provenance telemetry that excludes precise Home/Work, route coordinates, push tokens and provider secrets. Evidence: station payloads expose source type, official-live status and freshness; station/score contexts expose provider/cache provenance without user route labels, route coordinates, push tokens or provider secrets.
- [x] Add prediction back-testing API contract with metric summary and accuracy-claim gates.
- [x] Add durable prediction storage adapter for Neon/Postgres with safe memory fallback.
- [x] Provision production Neon/Postgres and set `DATABASE_URL` plus `PREDICTION_BACKTEST_WRITE_TOKEN`.
- [x] Set `ALERTS_WRITE_TOKEN` in production before backend alert writes are enabled.
- [x] Add backend no-cycle-signal states for unsupported fuels, regions and sparse histories.
- [x] Document data retention rules for saved routes, recents, prediction back-tests and alert records. Evidence: `DATA-RETENTION-RULES.md` maps current local/backend limits, launch gaps and no-sensitive-logs rules.
- [x] Add backend retention purge automation for invalidated push devices, disabled saved routes, old alert evaluations and old prediction back-tests before public backend alert sync. Evidence: protected `/api/internal/jobs/retention-cleanup` supports dry-run and enforced cleanup; regression covers expired-only deletion.
- [x] Add explicit saved-commute delete controls and backend saved-route delete sync before public backend alert sync. Evidence: Account remove control cancels local reminders, calls backend DELETE when configured and keeps the row if backend deletion fails; backend regression covers token-gated delete.
- [x] Complete privacy policy and store data-safety preparation against actual v1 behaviour. Evidence: `PRIVACY-POLICY.md`, `STORE-DATA-SAFETY.md` and `PRIVACY-PUBLISHING-CHECKLIST.md` cover location, notifications, saved routes, recents, alert identity, push tokens, prediction back-tests, retention, launch caveats and publication gates.
- [x] Add deployable privacy policy page to the web demo. Evidence: `web-demo/privacy.html` and the demo header link expose the release-candidate policy without claiming it is already published for store review.
- [ ] P1 privacy hardening: assess whether `/api/geocode` should move from GET query parameters to a POST body, or confirm hosting/proxy logs do not retain full geocode query strings containing user-entered addresses. Current app code avoids logging raw geocode queries and the retry/privacy regression guard covers application logging, but request URLs may still be captured by infrastructure logs.
- [ ] Add the confirmed privacy contact method and link the hosted privacy policy from Apple App Store and Google Play listings. Stable URL is live at `https://fuel-path.vercel.app/web-demo/privacy`; production deploy `dpl_DKiwudtscqXkNp61mFzoxwAoJXux` verified the policy returns HTTP 200 with the current v1 copy and production security headers. Store publication remains blocked until contact and listing links are confirmed. `scripts/check-beta-readiness.mjs` now treats manual confirmation flags as audit-only, so this gate requires concrete privacy contact, Apple listing, Google Play listing and fresh reviewed disclosure evidence; release reference dates must also be non-future and fresh.
- [x] Add break-it test evidence to every new backlog item before marking it done. Evidence: `BACKLOG-EVIDENCE-MATRIX.md` maps current completed, blocked and launch-gated backlog items to regression tests, stress checks, blocker notes or external validation requirements.
- [x] Prepare Apple/Android store readiness plan, including privacy disclosures and API usage constraints.
- [x] Add provider terms readiness gate for public live-price claims. Evidence: `npm run check:provider-terms` reads local or hosted `/api/status`, reports terms blockers and access blockers separately, and fails when `--enforce-public-launch` is used while configured provider terms are missing. Regression covers missing NSW/ACT/QLD terms and all-configured-terms-confirmed state.

## External Blockers

- Real validation sessions need recruited participants and scheduled sessions.
- National provider usage, caching, attribution and public-sharing permissions need confirmation by jurisdiction.
- FuelCheck app/commercial usage, caching rules and public-sharing permission need API.NSW confirmation.
- QLD usage constraints still need confirmation against the licence terms before public/commercial launch; production QLD live routing is fail-closed until `FUEL_PATH_QLD_USAGE_TERMS_CONFIRMED=1`.
- VIC Servo Saver live adapter is implemented and configured; public claims still need provider evidence to pass the release gate.
- SA Fuel Pricing Information Scheme access is granted; production token must be configured server-side before live SA prices appear.
- TAS access path, live API.NSW v2 payload access and backend adapter implementation are validated; production TAS live routing is fail-closed until `FUEL_PATH_TAS_USAGE_TERMS_CONFIRMED=1` is set after usage/caching/attribution confirmation.
- NT MyFuel data/API access needs confirmation before implementation.
- Google Places production autocomplete still needs real Google Cloud key restriction, budget alert confirmation and device validation before live enablement. Code gates now fail closed unless these confirmations are present alongside durable quota storage and a tiny daily cap.
- Current local machine can run Android ARM64 AVD validation and iOS simulator validation. Historical physical Android render/performance passed on Pixel 9 Pro `49231FDAP0017N`, but the current localParity APK still needs fresh physical-device proof; a second lower-end Android pass would improve confidence before broad public performance claims.

## Done Gate

- Every new backlog item needs user story, success metrics and break-it test evidence.
- Every implementation needs a backend or product decision rule, plus a UX surface that shows the rule outcome, limitation or blocked state.
- Use `NATIONAL-TESTING-REGIME.md` for feature, integration, accessibility, performance and post-deploy checks.
