# Fuel Path Backlog Evidence Matrix

Last updated: 1 July 2026

## Rule

Every new backlog item needs break-it evidence before it is marked done. Evidence can be a regression test, stress script, static check, manual validation record or blocker note when external access is required.

## Current Evidence Coverage

| Backlog item | Break-it evidence | Status |
| --- | --- | --- |
| Current native readiness override | `docs/native-simulator-launch-readiness-2026-07-01.md` is the current native evidence summary. Source-level iOS simulator validation passes through `tmp/native-smoke/ios-validation-2026-06-30T23-16-23-867Z.md`; production smoke matrix passes after the combined `{ route, score }` mock-contract fix in `tmp/production-smoke-matrix-stress-2026-06-30T23-42-23-275Z.md`; Android still needs fresh physical-device smoke/performance evidence for the current localParity APK before beta readiness or native performance claims. | Current source of truth. Older rows below preserve historical evidence and should not be read as current beta readiness. |
| Monetisation validation instrumentation | Route savings cannot support monetisation until they are measured as behaviour: repeated route plans, saved commute creation, route-alert opt-ins and navigation-open events. `docs/monetisation-behaviour-instrumentation-plan-2026-07-01.md` defines the event contract and privacy bands; `docs/templates/MONETISATION-BEHAVIOUR-EVIDENCE.template.json` is the first evidence shape. `npm run check:monetisation-behaviour -- --evidence-json docs/templates/MONETISATION-BEHAVIOUR-EVIDENCE.template.json --allow-blocked` blocks empty evidence, and `tests/api/monetisation-behaviour-evidence.test.js` proves sensitive fields, missing event evidence and threshold failure are rejected. Required dated evidence still needs real behaviour from commuter/high-frequency users and at least one small-operator pilot or willingness-to-pay signal. | Open P0 validation item. Do not use interest, positive feedback or concept fit as monetisation readiness evidence without measured behaviour. |
| B2B/fleet route fuel savings report | A shareable B2B/fleet-lite artefact is needed before building fleet SaaS: 5-10 recurring routes, approved-brand/fleet-card assumptions, pump price versus adjusted price, detour cost, provider/source limitations and no guaranteed-savings claim. `docs/fleet-lite-route-savings-report-validation-2026-07-01.md` defines the pilot boundary; `docs/templates/FLEET-LITE-SAVINGS-REPORT.template.md` is the first sample report artefact. Break-it evidence should include a completed sample report or generator fixture plus a review note proving policy, data coverage and savings wording stay cautious. | Open P1 validation item. Treat as a concierge/pilot artefact until at least one operator gives concrete willingness-to-pay or pilot follow-up evidence. |
| EV provider-readiness boundary | EV route planning must stay prototype/cautious until provider pricing, coverage, availability semantics and commercial terms are approved. `docs/ev-provider-readiness-boundary-2026-07-01.md` defines allowed copy, blocked copy and the provider evidence checklist. Break-it evidence should include a provider-readiness checklist for pricing, rate limits, Australian/NT coverage, connector and power fields, live availability semantics, attribution, caching and commercial display rights, plus a static or user-facing copy guard that blocks unsupported live-availability, tariff or route-optimisation claims. | Open P2 boundary item. Keep EV discovery labelled as directory/prototype where evidence is missing. |
| Stabilisation pass before new feature expansion | Web demo review found map station logos visually degraded into oversized/clipped marker images even though list icons still rendered. Root cause was a fragile marker-image contract: Leaflet/map image sizing could leave child logo images at their source dimensions inside a 22px marker cell, and stale local cache keys kept the old JS/CSS visible after patching. `web-demo/app.js` now emits bounded inline dimensions for marker logo images, `web-demo/styles.css` hard-scopes price-pin logo sizing, and `web-demo/index.html` versions local CSS/JS cache keys. Browser verification on `http://localhost:4175/web-demo/` confirmed 136/136 marker logos are bounded to the marker cell. `tests/api/web-demo-safety.test.js` now guards visible cache keys and bounded brand-mark sizing. | Added as open P0 stabilisation rule. Do not add more feature scope until the currently visible Plan, Nearby, Account and native smoke surfaces have been rechecked and any lost visual contracts are restored or explicitly parked. |
| TAS FuelCheck adapter through API.NSW v2 | Mocked provider-routing coverage plus the dated access record in `docs/03-provider-data/evidence/historical/PROVIDER-ACCESS-READINESS-2026-06-26.md`. Current readiness must be checked through `docs/03-provider-data/provider-readiness.md`. | Covered for internal validation; public use remains terms-gated. |
| NSW/ACT, QLD and TAS production usage gates | Provider-routing tests cover fail-closed live routing until usage terms flags are set. `/api/status` now exposes `fuelProviders.publicClaims`, separating technical live adapter capability from public live-price claim readiness; it blocks when terms are missing and when terms flags are set without `FUEL_PATH_PROVIDER_TERMS_EVIDENCE_CONFIRMED=1`. `/api/status` also exposes `sourceScope`, making the legacy `defaultSource` field explicit as a coarse diagnostic only, and `releaseReadiness.publicBeta`, which keeps provider terms, physical-device validation, iOS validation, privacy/store evidence and support readiness visible as external blockers. `tests/api/provider-routing.test.js` verifies the status source-scope/public-claims contract and that provider or Google map key values do not appear in the status JSON. `npm run check:provider-terms -- --api-base https://fuel-path.vercel.app` reports NSW, ACT, QLD and TAS terms blockers separately from evidence blockers and VIC/NT access blockers. `docs/templates/PROVIDER-TERMS-EVIDENCE.template.json` defines the written-evidence fields needed before treating live-price terms as confirmed, including terms acceptance dates, cache durations and attribution/disclaimer wording. `tests/api/provider-terms-readiness.test.js` covers missing terms, flags-without-evidence blocking, vague evidence rejection and all-configured-terms-with-evidence pass state. | Covered as a release and status-surface gate; written terms confirmations remain external. |
| Durable Google Places fallback quota storage and billable controls | `tests/api/google-provider-mock.test.js` covers durable cap exhaustion before provider calls, cache reuse and disabled paid fallback. `tests/api/lookup-readiness.test.js` and `tests/api/lookup-deployment-stages.test.js` now require durable quota storage, tiny daily cap, confirmed Google Places key restrictions and confirmed budget alert before enabled billable fallback can be ready. | Code gates covered; real Google Cloud restriction and budget alert confirmation remain external. |
| Address lookup source and confidence UX | `npm run verify` includes `location suggestions show honest confidence chips`; rendered Plan smoke on `http://127.0.0.1:8081/` now covers `Needs confirmation` validation rows and `Street/area only` local/regional fallback rows, with detail copy explaining that street/area rows are not exact addresses. | Covered for Plan, Nearby and Account code paths; physical-device validation still pending. |
| Hosted G-NAF production gate | Oracle-hosted G-NAF API has 16,905,824 rows, search indexes, bearer-token rejection and exact smoke evidence in `tmp/gnaf-hosted-readiness-2026-06-19T20-59-public-api.clean.json`. Production `/api/status` now reports lookup readiness `ready` with public exact-address claims allowed. `tests/api/gnaf-hosted-check.test.js` covers API-mode row-count enforcement, search-index readiness, bearer-token rejection and exact top-ranked smoke searches across real G-NAF records in NSW, ACT, VIC, QLD, WA, SA, TAS and NT. | Covered for production readiness. Keep benchmark evidence fresh within the 30-day readiness window. |
| Hosted G-NAF storage/cost review | `npm run review:gnaf-hosted-storage -- --load-plan tmp/gnaf-hosted-load-plan-2026-06-19T16-25-01-136Z.json --require-passed` produced `tmp/gnaf-hosted-storage-review-2026-06-19T16-25-39-760Z.md`. `tests/api/gnaf-hosted-load-plan.test.js` covers passing Oracle Always Free review, oversized paid-risk volume blocking and insufficient load workspace blocking. | Covered for storage/cost only. It confirms the national load attempt fits the reviewed Always Free assumptions; it does not prove hosted deployment or public launch. |
| Hosted G-NAF national-load plan | `npm run plan:gnaf-hosted-load` inspects local raw ZIP, local national SQLite, hosted row count, hosted target kind, hosted search-index state and latest storage review without writing data. Latest report: `tmp/gnaf-hosted-load-plan-2026-06-19T20-38-10-549Z.md`, showing 16,905,824 local rows, 80,000 hosted rows, target kind `neon`, no missing hosted indexes and reviewed estimated hosted storage of 17.5-35 GB with status `ready_to_load`. The generated national-load command includes `--storage-review`, `--progress-json` and `--run-id`. `tests/api/gnaf-hosted-load-plan.test.js` covers missing local SQLite, storage-review-required, hosted-target-required, readiness-index parity and storage-review handoff states. | Covered as a go/no-go plan. National hosted load is ready only after the intended Oracle/dedicated target is configured; the actual hosted load remains undone in current local evidence. |
| Hosted G-NAF national-load progress/recovery guard | `scripts/load-gnaf-raw-postgres.mjs` refuses unbounded hosted loads without `--progress-json`, writes progress on dry setup, records completed states as each state finishes, writes `failed` status if setup fails, resumes with `--resume-progress-json`, skips completed states and refuses resume with `--reset`. `tests/api/gnaf-load-runner.test.js` covers required progress manifest, dry-run manifest output, setup-failure evidence, state skipping and reset refusal. | Covered for progress, audit and state-level resume evidence. The actual national hosted load remains undone. |
| National From/To autocomplete fallback under provider outage | `tests/api/geocode-resilience.test.js` covers state-suffixed capital/airport hints, disambiguated POI/airport prefixes, `Artamon` typo correction and national regional towns without provider calls; `npm run test:geocode-route-fields:local` resolves 52/52 endpoints and 100/100 unique route pairs under forced Nominatim rate limiting, reports both any-match and top-1 prefix character counts, and fails if route-pair uniqueness regresses. | Covered for local no-cost fallback; broad 3-character prefixes intentionally remain broad, and hosted national G-NAF remains a separate launch item. |
| Exact-address launch readiness | `npm run test:geocode-exact-address-readiness:local` compares seed-only/current configured lookup with a temporary all-jurisdiction G-NAF SQLite fixture. Fixture G-NAF resolves 16/16 exact labels with 0 provider calls; current seed mode resolves 2/16 and is explicitly reported as not ready for a public exact-address claim. | Covered as a readiness gate; launch remains blocked until hosted national G-NAF is loaded and passes current-configured mode. |
| Exact-address From/To route-field stress | `npm run test:geocode-route-exact-addresses:local` runs 100 unique route pairs across the shared 16-case national exact-address fixture. Latest report: `tmp/geocode-route-exact-address-field-stress-2026-06-19T19-38-49-283Z.md`. Temporary G-NAF fixture mode resolves 16/16 exact labels and 100/100 unique route pairs with no provider calls; current configured mode still shows only the two NSW seed addresses exact. | Covered for route-field mechanics; production precision and characters-to-correct-suggestion remain hosted benchmark evidence, not fixture evidence. |
| Local national G-NAF SQLite broad-prefix performance guard | `tests/api/gnaf-address-index.test.js` covers broad-prefix skipping, road/unit token cleanup and large unit-number preservation for large SQLite indexes. `npm run test:geocode-local-national-address-smoke` passed 32/32 real sampled national address rows from `data/gnaf/build/gnaf-addresses-national.sqlite`, with p90 30 chars and external provider blocked. Latest report: `tmp/geocode-hosted-national-benchmark-2026-06-19T17-26-43-956Z.json`. | Covered for local developer precision/performance evidence. It is not a hosted/public launch gate and still shows local national SQLite is too slow for production autocomplete UX. |
| Hosted national lookup benchmark | `scripts/geocode-hosted-national-benchmark.mjs` builds a 600-address plus 300-POI benchmark for a hosted `/api/geocode` endpoint; `tests/api/geocode-hosted-benchmark.test.js` covers the HTTP contract, address sampling, benchmark artefact output and threshold enforcement against a mock hosted API. Public hosted benchmark `tmp/geocode-hosted-national-benchmark-2026-06-19T23-04-43-862Z.json` passed 900/900 top matches, with 600/600 G-NAF addresses, 300/300 POIs, p90 any-match chars 18 overall, 18 for addresses and 3 for POIs. | Covered for current production launch evidence. |
| Lookup readiness deploy gate | `/api/status` now exposes `geocoding.lookupReadiness`; `npm run check:lookup-readiness -- --api-base https://fuel-path.vercel.app` passes with status `ready`, hosted API mode, 16,905,824 rows, exact smoke passed, hosted benchmark passed and no blockers. `tests/api/lookup-readiness.test.js` covers local-not-ready, evidence-ready, future-dated hosted benchmark rejection, billable-fallback-blocked states and all-paid-controls-ready state. | Covered as a fast production deploy gate. |
| Staged lookup deployment plan | `npm run plan:lookup-deploy` turns the latest timestamped release summary plus optional readiness JSON into local-test, hosted-preview, production-smoke and paid-fallback stages. Latest report: `tmp/lookup-deployment-stages-2026-06-19T23-48-38-923Z.md` is `ready`, with next stage `paid_fallback` and no blockers. `tests/api/lookup-deployment-stages.test.js` covers missing hosted evidence, fail-fast required stages, ready production evidence, paid-fallback quota/cap/restricted-key/budget-alert blockers, all-controls-ready state and ignoring named unit-test summaries. | Covered as a release sequencing guard; paid fallback remains disabled pending real Google Cloud restriction, tiny-cap and budget-alert confirmation. |
| Lookup readiness evidence publishing | `npm run publish:lookup-readiness-evidence -- --hosted-check <json> --benchmark <json>` generates the env contract consumed by `check:lookup-readiness`; `tests/api/lookup-evidence-publisher.test.js` covers passing handoff and failure on missing exact smoke, failed benchmark thresholds, missing benchmark timestamp or future-dated benchmark timestamp. | Covered for generated evidence. Real hosted execution remains blocked until national hosted G-NAF/API is live. |
| Lookup release evidence summary | `npm run summarise:lookup-release-evidence` collects route-field, exact-address, hosted G-NAF load-plan, rendered Plan smoke, 600-prefix, hosted preview, hosted national benchmark and optional public lookup-readiness artefacts into one JSON/Markdown release decision. Latest report: `tmp/lookup-release-evidence-summary-2026-06-19T23-48-22-810Z.md` passed with local precision ready and public launch ready. `tests/api/lookup-release-evidence-summary.test.js` covers local-pass/launch-blocked, hosted readiness handoff, storage-review-required, ready-to-load, full launch pass, enforced launch failure, ignoring named fixtures and refusing local module-mode national benchmark artefacts as hosted launch evidence. | Covered as a release review surface. Hosted benchmark auto-resolution now requires HTTP mode, API base, 600 requested addresses, 300 requested POIs, at least 900 cases and at least 900 `/api/geocode` calls. |
| Local lookup-release runner | `npm run release:lookup-local` runs the local precision pack in order, including route fields, exact-address route fields, 600-prefix benchmark, hosted G-NAF load plan, auto-detected rendered Plan-field smoke when the local app is reachable, and release summary. Latest full runner report: `tmp/lookup-release-evidence-summary-2026-06-19T16-28-02-298Z.md`; latest refreshed release summary after street/area precision UX, resolver hardening, hosted-load progress guard, hosted-evidence resolver hardening and 100-pair rendered Plan stress: `tmp/lookup-release-evidence-summary-2026-06-19T19-35-28-046Z.md`. `tests/api/lookup-local-release-runner.test.js` verifies dry-run order, forced Plan smoke, reachable auto-inclusion and explicit opt-out. | Covered for repeatable local release evidence; public launch remains hosted-evidence gated. |
| Route-field early ranking precision | `tests/api/geocode-resilience.test.js` covers visible primary-name ranking over alias-only matches, generic regional town prefixes before airport/POI rows, explicit POI intent preservation, state-bearing labels for state-suffixed queries and street-locality fallback precedence. Latest `npm run test:geocode-route-fields:local` report: `tmp/geocode-route-field-stress-2026-06-19T19-23-49-555Z.md`. | Improved and now stable: route-field p90 top-1 chars is 8, route-pair top-1 max is 20 across 100 unique pairs, and the latest run resolves all 52 endpoints and 100/100 route pairs. MCG and city-plus-airport prefixes still need natural extra characters because broad city intent legitimately competes with POI intent. |
| Rural, remote and island lookup coverage | `npm run test:geocode-prefix-600:local` is a regression gate across NSW, ACT, VIC, QLD, WA, SA, TAS and NT; it resolves 600/600 top matches under forced provider rate limiting, including 120/120 `rural_remote_island` cases, with 0 wrong suggestions. | Covered for benchmarked local fallback; still needs hosted national G-NAF for exact-address launch coverage. |
| Hosted geocode preview smoke | `npm run test:geocode-hosted-preview -- --api-base <hosted-url>` writes JSON and Markdown release-smoke evidence for 20 high-risk exact-address, unit/slash, townhouse, rural, remote, island and POI cases. `tests/api/geocode-hosted-preview-smoke.test.js` covers pass, fail-after-artefact behaviour and non-GNAF hosted address-row diagnosis against a mock hosted API; `tests/api/lookup-release-evidence-summary.test.js` verifies the diagnostic blocker carries into the release decision. | Runner covered; real preview execution remains blocked until a hosted API target has national G-NAF coverage. |
| Partial exact-address UX | `mobile-app/scripts/check-map-camera-guards.mjs` verifies partial street address fields show quiet suburb/postcode guidance while noisy autocomplete errors remain suppressed. | Covered for Plan fields; this improves dead-air UX but does not improve backend match speed. |
| Exact-style address unsafe early street suggestions | `tests/api/geocode-resilience.test.js` covers short locality prefixes, street-only prefixes, titlecase partial state/locality tokens such as `Wa`, explicit uppercase state-code disambiguation such as `WA`, numbered street queries and cache separation for typed-vs-explicit state codes. `npm run test:geocode-prefix-600:local` now reports `wrongStreetTopBeforeAnyMatch`; latest run preserved 600/600 final top matches and reduced early wrong-street top suggestions from 3 to 0. | Covered for local fallback precision; ambiguous street input now waits for locality, postcode, explicit state code or hosted G-NAF before showing curated street hints. |
| Slow exact-address local-fallback UX | `tests/api/lookup-release-evidence-summary.test.js` covers release-summary attention items for precise-address and duplex/townhouse categories when p90 characters-to-correct-suggestion stays high. Latest `npm run summarise:lookup-release-evidence` reports the slow categories while keeping local fallback gates separate from public launch readiness. | Covered as release evidence; real UX improvement still depends on hosted national G-NAF or controlled provider autocomplete. |
| Bounded geocode cache memory | `api/_geocode.js` now caps the in-process geocode cache with `FUEL_PATH_GEOCODE_CACHE_MAX_ENTRIES`, defaulting to 500 and clamped to 1-5000. `tests/api/geocode-resilience.test.js` covers same-query cache reuse and max-entry eviction by forcing a one-entry cache, filling two unique provider lookups and proving the first lookup is evicted before reuse. | Covered for in-process cache growth. Durable/provider quota controls remain separate launch gates. |
| Route-planning lookup failure copy | `mobile-app/scripts/check-map-camera-guards.mjs` verifies raw lookup-busy wording stays out of the UI and that the mobile API client normalises provider/rate-limit failures before Plan route errors can render them. | Covered for mobile client copy; real provider outage behaviour still depends on backend status contracts. |
| Plan route pre-submit precision guard | `mobile-app/scripts/check-map-camera-guards.mjs` verifies address-like From/To input shows start/destination suggestion guidance and disables the Plan route CTA until the user selects a suggestion. Browser smoke covered state-only addresses, locality-qualified addresses, confirmed suggestion selection and successful route submission from confirmed points. | Covered for Plan form UX; native interaction pass still pending. |
| Validation-provider suggestion precision | `mobile-app/scripts/check-map-camera-guards.mjs` verifies address-like validation suggestions are ranked through `rankLocationSuggestions`, visibly labelled `Needs confirmation`, and rejected by direct geocode until explicitly selected. Browser smoke for `2 George Street Sydney NSW` confirmed numbered street rows rank above `Parramatta Childrens Court` and every validation address row warns that it is not an exact address match. | Covered for mobile web and shared client logic; true exact-address confidence still depends on hosted national G-NAF or a controlled production autocomplete provider. |
| Typed route endpoint precision | `mobile-app/scripts/check-map-camera-guards.mjs` verifies `geocodeAddress` rejects weak automatic area matches for address-like text unless the user explicitly chooses a suggestion. | Covered for Plan/Nearby direct geocode calls; full native interaction pass still pending. |
| Rendered Plan-field precision smoke | `npm run smoke:plan-fields` in `mobile-app/` runs 11 browser cases against the rendered Plan screen with deterministic mocked API responses. Latest report: `tmp/plan-field-browser-smoke-2026-06-19T19-28-51-139Z.md` with 11/11 passing against `http://127.0.0.1:8081/`, including disabled states, suggestion confirmation, validation-provider ranking, `Needs confirmation`, `Street/area only`, airport pair route submit and route-edit recovery. The script also preflights the local app URL and prints the Expo start command when the app is not reachable. | Covered for web-rendered mobile viewport. It does not prove native iOS/Android controls or production provider precision. |
| Rendered Plan-field route-pair stress | `npm run stress:plan-fields` in `mobile-app/` runs 100 unique national From/To pairs against the rendered Plan screen with deterministic mocked geocode, route and score responses. Latest report: `tmp/plan-field-browser-stress-2026-06-19T19-24-02-329Z.md` with 100/100 passing, 100/100 unique pairs, 52/52 endpoint coverage, all states and territories covered, 200 geocode calls and 4 route submits. | Covered for rendered web/mobile-viewport Plan selection resilience. It does not prove native iOS/Android controls or production provider precision. |
| Core route decision action contract | `api/_routeScoring.js` emits one clear timing action for the Plan result: fill on-route, fill with detour, locked wait, skip, range-first or no eligible signal. `tests/api/route-timing-advice.test.js` covers the action states, including range-first when every candidate is beyond reserve, and proves demo/sample tomorrow prices cannot trigger wait advice. `tests/api/wa-fuelwatch-v2.test.js` covers WA locked tomorrow price normalisation. | Covered for backend decision contract. Prediction/cycle guidance remains parked until measured accuracy evidence exists, and real users still need to prove the recommendation is understandable. |
| Discount wallet price certainty | Mobile pricing now keeps pump price, confirmed wallet price and possible lower price distinct. Possible lower prices carry `possibleLowerDisclosure`, use "possible only" and "not guaranteed" copy in Plan, Nearby and station rows, and are excluded from confirmed scoring when the discount is unconfigured or redeemed today. `mobile-app/scripts/check-map-camera-guards.mjs` protects the copy/contract, and `npm run verify` in `mobile-app/` passed typecheck, guard checks, web export and size budgets. | Covered for manual wallet copy and client scoring boundaries. Real discount-user comprehension still needs validation sessions. |
| Shared direct-address confirmation copy | `mobile-app/scripts/check-map-camera-guards.mjs` verifies `geocodeAddress` uses neutral confirmation copy while Plan keeps route-specific pre-submit hints. | Covered for shared mobile client copy; live Nearby interaction still needs native/manual pass. |
| Backend retention cleanup | `tests/api/alerts.test.js` covers expired-only deletion through the protected cleanup path. | Covered. |
| Mobile local data retention caps | `mobile-app/scripts/check-map-camera-guards.mjs` now verifies local route-data caps match `PRIVACY-POLICY.md` and `DATA-RETENTION-RULES.md`: recents stay capped at 8 through `normaliseRecentLocations`, saved commutes stay capped at 20 before persistence, Plan exposes clear/remove recent controls, Account exposes saved-commute removal, and backend alert identity uses generated `local_` / `device_` IDs without contact, vehicle-rego or email fields. | Covered for static mobile storage contracts; real deletion behaviour still needs native/manual validation on device storage. |
| Saved-route alert outcome contract | `api/_alertEvaluation.js` now returns flat user-facing `outcome`, `outcomeLabel` and `outcomeSummary` fields for send alert, watch only, skip alert, quiet today and range-first outcomes. `tests/api/alerts.test.js` covers sendable alerts, paused quiet-day routes, missing permission/token watch-only states, unavailable route scoring, pending provider, stale price, saving below threshold, detour above threshold and range-first outcomes while push delivery remains disabled unless the environment gate is explicitly enabled. | Covered for backend alert decision clarity. Push delivery and commuter usefulness still require native/device and real-user validation. |
| Saved-commute delete controls and backend delete sync | Backend token-gated delete regression and mobile verification cover the local removal path. | Covered; native physical-device pass still required. |
| Privacy policy and store data-safety preparation | `PRIVACY-POLICY.md`, `STORE-DATA-SAFETY.md`, `PRIVACY-PUBLISHING-CHECKLIST.md` and `web-demo/privacy.html` describe current behaviour and publication gates. Production deploy `dpl_DKiwudtscqXkNp61mFzoxwAoJXux` verified `https://fuel-path.vercel.app/web-demo/privacy` returns HTTP 200 with production security headers; `tests/api/privacy-publication.test.js` keeps the Vercel build publishing the page and styles, requires the visible store-listing gate and blocks the old `production URL` overclaim while final store/privacy evidence is missing. `scripts/check-store-publishing-readiness.mjs` now gives a dedicated store/privacy evidence gate for the concrete monitored privacy contact, Fuel Path-owned public privacy policy URL, policy source containing the final contact, Apple listing, Google Play listing, Apple privacy review, Google Data Safety review, provider limitation disclosure, support process, review date, accountable reviewer, dated source-like review references and fresh review evidence. Review reference dates must not be future-dated or older than 30 days by default; custom freshness windows are capped at 90 days. Privacy policy URLs and privacy-contact URLs must be Fuel Path-owned, not third-party hosted pages or generic public forms. Support process evidence must reference the support runbook or a runbook review source, not just a dated inbox confirmation. `scripts/check-beta-readiness.mjs` consumes the same store gate, rejects placeholder/example/public-personal contact evidence, policy sources that still carry the final-contact-missing warning, placeholder store listing identifiers, App Store slug mismatches and Google Play package mismatches against `mobile-app/app.json`, generic reviewers and vague dated review notes, keeps manual confirmation flags as audit-only, and now passes bounded custom store review freshness windows through to the store checker instead of silently falling back to the default 30-day window. `tests/api/store-publishing-readiness.test.js` and `tests/api/beta-readiness.test.js` cover concrete-value pass, template block, non-public policy URL block, third-party-hosted policy URL block, policy-contact publication block, generic public contact URL rejection, Fuel Path-owned contact URL acceptance, placeholder listing identifier block, App Store slug mismatch, package mismatch block, personal inbox and generic reviewer block, missing review metadata block, missing release-review references, vague or wrong-category dated review references, inbox-only support-process reference rejection, stale review evidence, custom beta wrapper freshness, excessive freshness-window rejection, future/stale release-review references and flag-only block cases. | Stable hosted policy path covered; monitored contact method, store-listing links and fresh reviewed store disclosure evidence remain blocked until real release values are supplied. |
| Provider terms evidence | `docs/templates/PROVIDER-TERMS-EVIDENCE.template.json` plus `npm run check:provider-terms -- --evidence-json <file> --enforce-public-launch` require configured NSW/ACT, QLD and TAS providers to have accepted usage, caching, attribution/disclaimer and commercial-use evidence before public live-price claims. Evidence must include non-future acceptance dates, cache limits inside recorded obligations, concrete provider/source-specific attribution wording, a dated source-like evidence reference that names the matching provider/source family, and an `evidenceSource` file containing the reference date plus matching provider family. The gate rejects boolean-only claims, generic attribution claims, state-only ticket names, vague ticket names, missing source files and source files that appear to contain provider API secrets. Evidence reference dates must not be future-dated, older than the recorded terms acceptance date or older than 90 days by default. Custom provider-terms freshness windows are capped at 365 days. `tests/api/provider-terms-readiness.test.js` covers missing flags, flag-without-evidence, vague evidence fields, generic attribution wording, future acceptance dates, vague evidence references, backwards-dated evidence references, future-dated evidence references, stale evidence references, excessive freshness-window rejection, wrong-provider evidence references, state-only provider references, missing source files, source-file secret leakage and full pass state. | Blocked until real provider terms evidence is held for NSW/ACT, QLD and TAS. VIC and NT remain separate access/schema blockers. |
| Support readiness | `docs/02-build-release/SUPPORT-RUNBOOK.md` defines intake, triage, escalation, privacy/data requests, provider-price issues, native-device issues and evidence logging. `npm run check:support-readiness -- --support-contact <contact> --support-owner <owner> --reviewed-at <date>` verifies the runbook shape plus concrete monitored contact, accountable owner, non-future review date, fresh review age and complete evidence-scope coverage before support readiness is treated as clear. Required runbook sections must be real markdown headings, the claimed support contact and owner must appear in the runbook or reviewed support source, and the evidence scopes must cover privacy/deletion, provider-price, map/location and alert-failure paths, not just one support topic. Custom freshness windows are capped at 90 days. Support-contact URLs must be Fuel Path-owned, not generic public forms. It rejects public personal inboxes and generic owner labels. `scripts/check-beta-readiness.mjs` now consumes that support gate whenever store evidence claims the support process is ready, passes bounded custom support review freshness windows through to the support checker, and reports `support_readiness_not_ready` if the contact, owner, review date freshness, contact/owner publication or runbook coverage is missing. It also reports `support_contact_mismatch` if the confirmed privacy contact and support contact differ; absent contacts are not reported as matching. Contact comparison is URL-aware for Fuel Path-owned HTTPS contacts, so harmless query strings or trailing slashes do not false-block the same `/privacy#contact` channel, while different contact paths such as `/privacy#contact` versus `/support#contact` still block. `tests/api/support-readiness.test.js` and `tests/api/beta-readiness.test.js` cover missing contact/owner/date, claimed contact/owner missing from the runbook, vague owner and personal inbox rejection, generic public contact URL rejection, Fuel Path-owned contact URL acceptance, incomplete runbooks, prose-only section mentions, incomplete evidence-scope coverage, future-date rejection, stale-date rejection, custom beta wrapper freshness, excessive freshness-window rejection, support/privacy contact mismatch, URL-equivalent support/privacy contact match, different-path URL mismatch, absent-contact comparison, the pass shape and beta-gate propagation. | Runbook structure covered; real monitored support contact and owner evidence remain external. |
| Web demo validation boundary | `web-demo/index.html` now labels the first viewport as a controlled internal demo while Phase 0 beta gates remain blocked, explicitly naming provider terms, iOS validation, store/privacy evidence and support readiness while acknowledging Android physical performance evidence is captured for the current preview. Its Account notification controls are preview-only rather than live push, cycle-alert or discount-reminder claims. Station-panel price-alert actions also say preview only and no alert was scheduled or sent, instead of claiming a price alert was set. `tests/api/web-demo-safety.test.js` protects the visible boundary, parked notification copy and station action copy. | Covered for the local web demo. iOS validation, provider terms, store/privacy evidence and support readiness remain open. |
| Web and mobile route-switch correctness | Browser review found two high-risk saved-route bugs: selecting `Canberra to Sydney` could update the route chip while leaving the old Parramatta recommendation, 23.9 km route summary and saved-route alert visible, and the saved-route suggestion menu could close on input blur before the click handler applied the selected route. `applySavedCommute` now clears `state.plannedRoute` and `state.activeRouteKey` before rendering, and `#savedRouteSuggestions` applies `[data-saved-route]` selections on `pointerdown` before blur removes the menu. Current Playwright replay confirmed the live demo shows the Canberra chip, 281.5 km route summary, Goulburn recommendation and `Canberra to Sydney` saved-route alert after the suggestion is selected. `tests/api/web-demo-safety.test.js` guards both the web route-cache invalidation and the pointerdown-before-blur contract. `mobile-app/scripts/check-map-camera-guards.mjs` now also verifies mobile Plan route requests ignore stale success/error responses after edits, clear route result state through `markRouteEdited`, and saved-commute switches replan from the selected commute's From/To points instead of stale field state. | Covered for local web demo and mobile static route-state contracts. Native saved-route switching still needs physical-device validation. |
| Web demo map marker accessibility noise | Browser snapshot showed dozens of Leaflet price pins exposed as early map buttons before the Plan controls. Leaflet price pins now use `keyboard: false` and `makeMapMarkerDecorative`, which sets `tabIndex=-1`, `aria-hidden=true` and removes duplicate `title` labels while keeping pointer map clicks. Playwright DOM inspection confirmed representative route and Nearby markers have `tabIndex: -1`, `ariaHidden: "true"` and no `title`; `tests/api/web-demo-safety.test.js` guards the decorator contract. | Covered for local web-demo Leaflet pins. A full assistive-tech pass remains a native/device validation item. |
| Mobile map marker semantics | `mobile-app/src/components/StationMap.web.tsx` keeps Leaflet station pins decorative (`keyboard: false`, empty `alt`, `aria-hidden="true"` inside the marker HTML and no duplicated title), leaving station rows and selected-station panels as the semantic controls. `mobile-app/src/components/StationMap.native.tsx` now applies `accessibilityElementsHidden` and `importantForAccessibility="no-hide-descendants"` to native station markers and removes their duplicate title/description callout text, while `StationRow` exposes a labelled button with selected state, price, distance, source/freshness and discount context. `mobile-app/scripts/check-map-camera-guards.mjs` guards both web and native contracts, and `npm run test:map-camera` passes. Expo SDK v56 documentation was checked before editing mobile-app code. | Covered as a static web/native accessibility contract. Real screen-reader behaviour still needs physical assistive-tech validation, especially on iOS. |
| Mobile screen architecture growth guard | `mobile-app/scripts/check-screen-complexity.mjs` records the current large-screen line counts and now also caps screen-local `useState`, `useEffect`, `useMemo`, `useCallback` and `useRef` counts for `PlanScreen.tsx`, `NearbyScreen.tsx` and `AccountScreen.tsx`. `SavedCommuteShortcuts` now owns the saved-commute shortcut strip outside `PlanScreen`, `QuickPlaceShortcuts` owns the Home/Work/recent route shortcut panel, `DecisionEvidencePanel` owns the recommendation evidence/trade-off metrics, `RouteAddressSuggestions` owns route address suggestion rows plus typed-address precision hints, `PlanRouteEditorCard` owns the Plan address inputs, current-location control, suggestion rendering, fuel picker and plan button, `PlanRouteSheet` owns the minimised map sheet, station detail panel, recommendation summary, empty states, save-commute action and suggested-stop list, and `useRouteAddressSuggestions` owns address suggestion loading/error/results plus Places-style session-token reset. Plan route edit handlers now share one `reopenRouteEditor` reset path for route controls, station detail and sheet minimisation while leaving stale-response protection in `PlanScreen`. `NearbyLocationSearch` owns Nearby location input, recent/suggested-location rows, evidence chips and current-location control; `NearbyStationSheet` owns the bottom sheet, selected-station card, sort controls, list and map recovery affordance; `useNearbyLocationSearch` owns Nearby query, recent locations, suggestion loading/error/results and Places-style session-token reset; `VehicleFuelCard` owns Account vehicle/fuel settings; `DecisionRuleCard` owns lightweight saving/detour rule controls; `SavedPlacesCard` owns the Home/Work wrapper plus saved-commute quick actions; `SavedPlaceEditor` owns Account saved-place search, current-location capture, map-centre refinement and saved-place confidence chips; `DiscountWalletCard` owns wallet selection versus redemption state; `PolicyModeCard` owns policy-mode approved-brand settings; `SavedRouteAlertsCard` owns notification permission copy, saved-route watch/remove actions, sync timestamps and per-route saving/detour/tank rules; `WeeklyReportCard` owns the driver-facing fleet-lite summary and no-payroll/no-admin boundary. This reduces Plan from 2070 to 729 lines, Nearby from 1253 to 468 lines and Account from 1361 to 115 lines while preserving saved-route, quick-place, recommendation evidence, address precision, location search, saved-place editing, discount wallet, policy mode, saved-route alerts, fleet-lite reporting and map/stops recovery behaviour. `npm test` runs `npm run arch:screens` before the map-camera guard, so future screen-local feature work has to extract route orchestration or settings modules instead of adding more behaviour to the large screens. Latest guard output locks Plan at 729 lines / 15 state hooks / 1 effect / 2 refs, Nearby at 468 lines / 14 state hooks / 3 effects / 1 ref and Account at 115 lines / 0 state hooks, with the current effect/memo/callback/ref counts also capped. | Partially covered: seven Plan extractions, one Plan orchestration cleanup, three Nearby extractions and eight Account extractions are done and guarded. Deeper `SavedPlaceEditor` lookup hook extraction and route/station orchestration splits remain open P2 cleanup work. |
| Native alert permission and push-token claim boundary | Review found native alert permission, saved-route alert labels and Expo push-token success copy could sound production-complete while beta readiness is still blocked by physical-device, iOS, store/privacy and support evidence. `routeNotifications` now says permission is enabled on the validation build and that the push token is ready for backend alert sync; `AccountScreen` labels the button `Permission enabled` rather than `Alerts enabled`, route toggles say `Watching` rather than `On`, and Plan saved-route chips say `Watching route` / `Not watching` rather than `Alerts on`. `alertGateSummary` now says route watches are on-device while delivery still needs native push and backend evidence before beta. `mobile-app/scripts/check-map-camera-guards.mjs` blocks the older overclaiming strings. | Covered for native copy/static contract. Real push-token/backend-sync behaviour still requires physical-device validation and beta-readiness evidence. |
| Supabase database platform review | Architecture review after the Supabase recommendation found that the production G-NAF path is not a direct VM-to-database comparison: Vercel currently calls a token-protected Oracle-hosted API, while Postgres stays private on the VM. Supabase is a credible managed Postgres candidate for admin UX/backups and smaller app data, but the 16,905,824-row G-NAF lookup layer needs a like-for-like storage, cost, latency, connection-pooling, backup, operational-ownership and rollback benchmark before migration. `scripts/plan-gnaf-hosted-load.mjs` detects Supabase connection strings as target kind `supabase`, carries `targetDecisionReviewed`, and keeps Supabase plans in `review_required` with `supabase_migration_review_not_confirmed` until `--confirm-supabase-migration-reviewed` or `FUEL_PATH_SUPABASE_MIGRATION_REVIEW_CONFIRMED=1` records that decision. `docs/templates/DATABASE-PLATFORM-REVIEW.template.json` and `npm run check:database-platform -- --evidence-json docs/templates/DATABASE-PLATFORM-REVIEW.template.json --allow-blocked` now make that review concrete: the checker blocks until Supabase evidence proves full row load, required indexes, 900-case benchmark parity, latency, storage/cost with official Supabase source references, server-side-only connection handling, backup/PITR decision, owner/monitoring/incident/budget plan and rollback back to Oracle. `tests/api/gnaf-hosted-load-plan.test.js` guards that the planner keeps Supabase visible instead of treating it as a generic target, and `tests/api/database-platform-review.test.js` covers empty-template blocking, direct mobile database access rejection and complete-evidence pass shape. | Added as an open P1 architecture decision. Current recommendation: keep Oracle API as the active G-NAF production path until a Supabase proof beats it on total operating risk, not just dashboard ergonomics. |
| Database secret hygiene | Review found live-looking local database connection strings in `.env.local`. The file is Git-ignored, but credentials that appear in local logs or chat/tool output should be treated as exposed. `scripts/check-secret-hygiene.mjs` now scans tracked plus untracked non-ignored files for live-looking Postgres URLs, Neon passwords, OpenAI keys, Google API keys and private key blocks while allowing local/test/example placeholders. `tests/api/secret-hygiene.test.js` covers placeholder allow-listing, credential rejection and untracked-file detection. | Added as open P0 operational hardening. Rotate affected database credentials and replace local env values before relying on them for production or shared work; tracked and untracked repo leakage is now guarded by `npm run check:secret-hygiene`. |
| Native readiness and mid-range Android performance | `mobile-app/NATIVE-VALIDATION.md` records the current split: Android emulator validation is locally available on `Fuel_Path_Arm64_API_35`, Android physical render/performance evidence is captured for the current preview APK on Pixel 9 Pro `49231FDAP0017N`, and full strict native readiness remains blocked by iOS `simctl` because this Mac is using CommandLineTools only. `mobile-app/scripts/native-device-readiness.mjs` detects Android SDK tools outside PATH, flags host/AVD ABI mismatch before launch, reports `sdkmanager`/`avdmanager` availability, auto-detects the local ignored JDK under `var/tooling/java` and has a `--require-physical-android` gate for real-device performance validation. `npm run native:android-avd-plan` passes for SDK root, Command-line Tools, Android 35 ARM64 image, ARM64 AVD and Android emulator. Android Expo Go render smoke reached the Fuel Path Plan surface after `routeNotifications` gated Android Expo Go away from the unsupported `expo-notifications` module; mobile guard checks keep that import guard in place. `npm run native:android-map-smoke` force-opens the exact local Expo URL, rejects Expo Go shell captures, captures Plan/Nearby/pan/Account screenshots and records `dumpsys gfxinfo`; latest corrected AVD evidence is `tmp/native-smoke/android-map-smoke-2026-06-20T01-21-51-852Z.md`. The same slice replaced deprecated React Native `SafeAreaView` with `react-native-safe-area-context` so Expo no longer overlays the warning screen. Android EAS preview build `f67da375-ad46-484d-8fc8-d5fc71a52e07` was built after the Plan default-centre and single-point camera fix, downloaded to `mobile-app/native-artifacts/fuel-path-preview-android-f67da375.apk`, passed the native APK budget at 70.68 MB / 76.29 MB and now has refreshed local parity physical installed-build performance coverage through `npm run native:android-performance-smoke`; latest physical evidence is `tmp/native-smoke/android-preview-smoke-2026-06-20T20-59-06-687Z.md` plus `tmp/native-smoke/android-performance-summary-2026-06-20T21-12-06-531Z.md`, status `passed`, with Plan/Nearby/Nearby-after-pan maps non-blank, no Maps warning lines, 186 frames, 1 janky frame, 0.5% janky, p95 8 ms and p99 11 ms. `npm run native:android-physical-readiness` and `npm run native:android-performance-smoke` refuse emulator-only/no-device runs so future performance evidence must still come from a physical Android device; `npm run native:android-performance-summary` separately reviews the smoke JSON and fails unless the evidence is from a physical Android device with an installed APK artifact name, adb serial/detail metadata, passed render/performance status, existing screenshot files for non-blank Plan/Nearby/Nearby-after-pan map captures and frame metrics inside claim thresholds. The APK signing SHA-1 is `cbd45223bd0f8a6791c9ab9d783ff895736ac39e` and SHA-256 is `8af30be0bd2ec9740cfa2ed85392a52b0bf78f61a18e4a3074068dca656ab368`. `npm run native:ios-simulator-plan` gives the matching iOS setup path and blocks cleanly while the Mac is using CommandLineTools without `simctl`. `npm run native:blocker-packet` writes combined Android/iOS blocker evidence; latest real packet `tmp/native-smoke/native-blocker-packet-2026-06-20T20-33-25-969Z.md` shows Android ready and only iOS blockers remaining: `ios:full_xcode_missing`, `ios:simctl_missing`, `ios:ios_runtime_missing` and `ios:ios_simulator_missing`. `scripts/check-beta-readiness.mjs` consumes the latest Android performance summary and latest real native blocker packet, blocks when no native blocker packet is present, skips synthetic test-generated native packets during auto-selection, rejects direct synthetic blocker packets with `native_blocker_packet_synthetic`, blocks native blocker packets older than 24 hours by default, caps custom blocker-packet freshness windows at 72 hours, rejects legacy `physicalDeviceEvidence` flags without `device.type: "physical"` metadata, rejects passed summaries with incomplete or missing-file physical map evidence, rejects physical reports with remaining attention items, rejects performance summaries whose source report, APK artefact or physical-device serial do not match the selected smoke report, requires a passed `ios-validation-*.json` report from `npm run native:ios-validation-report` with Plan, Nearby and Account screenshot evidence before clearing `ios_native_validation_missing`, and returns ordered multi-area next actions when provider, native, store and support blockers coexist. iOS setup tooling alone is not enough for beta readiness. `tests/api/native-device-readiness.test.js`, `tests/api/native-android-avd-plan.test.js`, `tests/api/native-android-performance-summary.test.js`, `tests/api/native-blocker-packet.test.js`, `tests/api/native-ios-simulator-plan.test.js` and `tests/api/beta-readiness.test.js` cover SDK-outside-PATH, missing Command-line Tools, actionable ARM64-image setup, existing ARM64 AVD, emulator-only physical-device blocking, physical-device pass shape, missing blocker-packet beta rejection, combined blocker-packet blocked/ready states, stale blocker-packet rejection, excessive freshness-window rejection, synthetic blocker-packet labelling and beta rejection, physical-performance summary pass/block cases, incomplete physical map evidence rejection, missing Android map screenshot file rejection, unauthorised physical Android blocker-packet rejection, legacy physical-flag rejection, attention-item rejection, mismatched summary-source rejection, summary-blocker precedence, CommandLineTools-only iOS, ready simulator setup cases, iOS validation report pass/block cases, missing iOS validation report rejection and mixed-blocker next-action ordering. | Android emulator, Expo Go render/navigation/pan smoke, installed preview APK launch smoke and physical Android render/performance are covered for the current preview APK. Push-token/backend-sync validation and iOS validation remain open. Full Xcode/simulator tooling remains blocked. |
| Real validation sessions | `VALIDATION-RECRUITMENT-PACK.md` and `VALIDATION-SESSION-WORKBOOK.md` prepare sessions. `docs/templates/VALIDATION-RESULTS.template.json` plus `npm run check:validation-results -- --results-json <file>` now enforce real-session-only validation gates: 5 of 7 net-saving understanding, 4 of 7 behaviour change, 4 of 7 top-recommendation understanding, 3 of 4 commuter/high-frequency saved-route alert demand with no spam feedback, 4 of 5 discount-user price-split understanding with no possible-offer guarantee confusion, and 1 small-operator pilot or willingness-to-pay follow-up. Completed real sessions also require a unique session ID, non-future session date, concrete evidence file, verbatim quote, concrete main trust objection, participant/moderator explanation notes for the top recommendation and net saving, minimum worthwhile saving and maximum acceptable detour; commuter/high-frequency rows require an alert use case; discount-user rows require price-split and possible-offer interpretation notes. Referenced evidence files must exist under the results file directory or `--evidence-root`, contain the matching session ID, matching session date and recorded quote, and avoid direct contact details. `tests/api/validation-results.test.js` covers empty-template blocking, synthetic-session rejection, incomplete session evidence, missing evidence file rejection, mismatched evidence file rejection, missing session-date evidence rejection, direct-contact evidence rejection, trust-objection enforcement, future-date rejection, duplicate IDs, explanation-note enforcement, net-saving-understanding threshold, alert-spam blocking and full pass state. | Blocked until participants are recruited, sessions run and a dated validation results file passes the checker. |
| VIC Servo Saver and NT MyFuel adapters | `api/_capabilities.js` and `tests/api/provider-routing.test.js` cover configured technical capability. `docs/03-provider-data/provider-readiness.md` keeps permission and public-claim checks separate. | Technical adapters covered; public claims remain provider-evidence gated. |

## Maintenance Rule

When a backlog item moves to done:

- add the failure mode or abuse case that could break the feature
- add the command, test, evidence file or external blocker proving that case was handled
- keep blocked external dependencies open in `TODO.md`
